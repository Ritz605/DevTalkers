import { HandleFilterPipe } from './handle-filter.pipe';

describe('HandleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HandleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
