import { HtmlSubStringPipe } from './html-sub-string.pipe';

describe('HtmlSubStringPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlSubStringPipe();
    expect(pipe).toBeTruthy();
  });
});
