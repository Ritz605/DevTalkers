import { HandleSortingPipe } from './handle-sorting.pipe';

describe('HandleSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new HandleSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
