import { GapFlexBoxColumnDirective } from './gap-flex-box-column.directive';

describe('GapFlexBoxColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new GapFlexBoxColumnDirective();
    expect(directive).toBeTruthy();
  });
});
