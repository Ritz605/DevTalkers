import { GapFlexBoxDirective } from './gap-flex-box.directive';

describe('GapFlexBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new GapFlexBoxDirective();
    expect(directive).toBeTruthy();
  });
});
