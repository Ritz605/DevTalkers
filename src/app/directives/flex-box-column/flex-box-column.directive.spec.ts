import { FlexBoxColumnDirective } from './flex-box-column.directive';

describe('FlexBoxColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexBoxColumnDirective();
    expect(directive).toBeTruthy();
  });
});
